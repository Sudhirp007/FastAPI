# Core FastAPI framework
fastapi

# ASGI server to run FastAPI
uvicorn

# For async database access (if using databases like PostgreSQL or MySQL)
databases

# SQLAlchemy for database ORM (Object Relational Mapper)
SQLAlchemy

# Pydantic for data validation and settings (used by FastAPI)
pydantic

# Requests library for making HTTP requests
requests

# Optional: If you are using Jinja2 for templating
jinja2

# Optional: If using OAuth2 authentication
python-jose

# Optional: If using JWT tokens
passlib
